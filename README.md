# adwall-remover-site
AdWall Remover — a lightweight browser extension that automatically removes anti-adblock overlays and restores page scrolling. Works silently in the background with zero configuration. No data collection, no permissions required. Available for Firefox, Chrome, and Edge.
